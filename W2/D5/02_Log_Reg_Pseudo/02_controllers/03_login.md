# Login Route

```py
@app.route('/login', methods=['POST'])
def login():
    # store the result of our validation in a variable

    # if that variable is false
        # redirect back to the form

    # store the result of our validation's id in session

    # redirect to the next place
```